# RIO One

Digital Operations Platform (Kanban board, attendance, leave, portfolio tracking, org chart) for a small team, backed by Supabase.

This repo contains only the published app (`index.html`) — no source data, migration exports, or internal tooling. Deployed via GitHub Pages.

To update: replace `index.html` with a new build and push to `main`.
